{
  "$schema": "task-health-registry.v1",
  "description": "Monitored tasks for the self-evolution log health rollup.",
  "tasks": [
    {
      "task": "build_dashboard",
      "statusFile": "status/build_dashboard.last-run.json",
      "freshnessHours": 36,
      "required": true,
      "allowNoop": false,
      "note": "Dashboard build task; should stay fresh on the daily loop."
    },
    {
      "task": "weekly_summary",
      "statusFile": "status/weekly_summary.last-run.json",
      "freshnessHours": 192,
      "required": true,
      "allowNoop": false,
      "note": "Weekly summary task; checked on a weekly cadence."
    },
    {
      "task": "auto_commit",
      "statusFile": "status/auto_commit.last-run.json",
      "freshnessHours": 36,
      "required": true,
      "allowNoop": true,
      "note": "Auto commit task; noop is acceptable when there are no changes."
    }
  ]
}
