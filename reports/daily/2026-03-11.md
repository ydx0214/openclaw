# Daily Evolution Report

- Date: 2026-03-11

## Done

- 建立 self-evolution-log 项目结构
- 建立 README、CHANGELOG、roadmap 与日报模板
- 创建每日自我进化定时任务

## Improved

- 把“每天执行但无沉淀”升级为“有项目、有记录、有历史”
- 后续每日优化结果可以统一落到项目内，便于回看

## Problems

- 目前定时任务仍写入旧的 `reports\self-evolution\` 目录
- 还没有接 Git 自动提交
- 还没有接 GitHub 远程仓库

## Next

- 更新 cron 任务输出路径到 `self-evolution-log`
- 增加 changelog 追加规则
- 视你需要接入 GitHub 自动提交

## Self-Evolution Update 14:31

- 学到的点：看板脚本如果把周报文件名写死为 2026-11.md，短期可用，但会把统计逻辑和某个具体文件绑定，后续很容易出现“脚本能跑但数据不再代表最新状态”的静默失真。
- 轻量优化：将 scripts\build_dashboard.ps1 改为自动选择 
eports\weekly\ 下最新的非模板周报文件，减少后续手工改名和遗漏风险。
- 下一步：看一下周报生成链路是否也存在类似的时间/文件名耦合点，并补一条最小校验规则。

## Self-Evolution Update 14:41

- 学到的点：不仅文件名会被写死，Git 推送目标分支如果写死为 `main` 也是同类环境耦合；在当前仓库没问题，但迁移到默认分支不同的仓库时会直接让自动提交链路失效。
- 轻量优化：将 `scripts\auto_commit.ps1` 从固定 `git push origin main` 改为先读取当前分支，再推送到对应分支，降低脚本对仓库默认分支的硬编码依赖。
- 下一步：检查周报生成和看板构建链路里，是否还有其他“路径/分支/文件名写死”的小耦合点可继续顺手清掉。

## Self-Evolution Update 14:51

- 学到的点：除了文件名和分支名，仓库远程地址写死也是常见的环境耦合；脚本在当前仓库能用，不代表复制到别的仓库后还能稳定产出正确看板信息。
- 轻量优化：将 `scripts\build_dashboard.ps1` 里的 GitHub 地址从硬编码改为优先自动读取 `origin` 远程地址，未配置时再回退为占位提示，减少仓库迁移后的手工维护点。
- 下一步：继续看周报统计项本身是否也有“分类名写死导致新增类别不显示”的轻耦合问题。

## Self-Evolution Update 15:01

- 学到的点：看板脚本如果把分类统计项写死为固定 5 类，周报后续新增类别时不会报错，但首页会静默漏显示，属于“结果看起来正常、信息却不完整”的隐性耦合。
- 轻量优化：将 `scripts\build_dashboard.ps1` 改为自动从最新周报里的 `### 分类名` + 计数块提取所有类别并渲染到首页，避免每次新增分类都手工改脚本。
- 下一步：看一下 `weekly_summary.ps1` 的分类关键词规则是否也需要补一个“新增类别时最小改动可扩展”的入口。

## Self-Evolution Update 15:11

- 学到的点：自动提交链路即使已经改成“按当前分支推送”，仍可能在新分支或新仓库上因为未设置 upstream 而失败；这类问题不是逻辑错误，而是 Git 环境初始化细节缺口。
- 轻量优化：将 `scripts\auto_commit.ps1` 补成“两段式推送”——先执行 `git push origin 当前分支`，失败后自动回退到 `git push --set-upstream origin 当前分支`，降低首次推送卡死概率。
- 下一步：看一下 `weekly_summary.ps1` 或其他汇总脚本里，是否还有类似“在新环境第一次运行才暴露”的初始化缺口。

## Self-Evolution Update 15:21

- 学到的点：weekly_summary.ps1 里的问题分类关键词仍是脚本内硬编码；虽然当前能用，但每次新增分类都要改脚本，属于“规则与逻辑耦合”的维护点。
- 轻量优化：新增 scripts\weekly_summary.categories.json 作为分类规则文件，并让 weekly_summary.ps1 按配置动态统计分类，后续补类别只改配置，不改脚本。
- 新方向探索：后续可以把“关键词分类”继续升级为“失败模式词库 + 样例库”，让周报不仅计数，还能沉淀更稳定的故障模式模板。
- 下一步：看 weekly_summary.ps1 是否还需要补一个“无规则文件时自动回退/报错更清晰”的最小保护。

## Self-Evolution Update 15:31

- 学到的点：把分类规则从脚本里抽到配置文件后，虽然更易扩展，但也引入了新的初始化风险——一旦规则文件缺失、为空或格式损坏，周报链路会直接失败；这类“为了解耦新增的单点依赖”需要最小回退保护。
- 轻量优化：给 `scripts\weekly_summary.ps1` 补了内置默认分类规则和清晰告警；当 `weekly_summary.categories.json` 缺失或解析失败时，脚本自动回退，不会因为单个配置文件问题阻断整条汇总链路。
- 新方向探索：后续可以把这类“关键配置 + 内置回退 + 明确告警”沉淀成一个通用脚本韧性模板，优先用于日报、周报、看板、自动提交这些日常无人值守链路。
- 下一步：看 `build_dashboard.ps1` 和后续定时任务里，是否还存在类似“关键输入缺失时直接中断”的点，并补统一的最小保护策略。

## Self-Evolution Update 15:41

- 学到的点：无人值守链路里，“最新周报不存在”也是高频初始化场景；如果首页看板构建强依赖周报文件，系统在刚初始化或周报暂未生成时就会整条中断。
- 轻量优化：给 `scripts\build_dashboard.ps1` 补了“无周报时自动回退”的最小保护；没有可用周报时不再报错退出，而是继续生成看板，并明确标记 `fallback: no weekly report yet`。
- 新方向探索：后续可以把“关键上游缺失时继续产出降级结果而不是直接失败”推广到更多定时任务，形成统一的韧性分级：正常 / 回退 / 告警。
- 下一步：看 `auto_commit.ps1` 是否也需要补“远程未配置时的降级提交策略或更清晰提示”，减少新仓库初始化摩擦。

## Self-Evolution Update 15:51

- 学到的点：无人值守 Git 链路里，`origin` 远程缺失是比推送权限失败更早、更常见的初始化问题；如果直到 `git push` 才暴露，用户只能看到笼统失败，而不知道其实是仓库还没接远程。
- 轻量优化：给 `scripts\auto_commit.ps1` 增加 `origin` 预检；在本地 commit 完成后、推送前先检查 `git remote get-url origin`，缺失时直接给出明确错误 `GIT_REMOTE_ORIGIN_MISSING`，降低排查成本。
- 新方向探索：后续可以把“脚本执行前先跑最小 preflight 检查（路径 / 分支 / 远程 / 关键输入）”沉淀成统一模板，用在日报、周报、看板、发布等无人值守链路。
- 下一步：看能否把这套 preflight 模板继续补到 `weekly_summary.ps1` 或其它定时脚本里，统一形成“先检查、再执行、失败可解释”的稳态流程。

## Self-Evolution Update 16:01

- 学到的点：无人值守自动提交流程里，origin 和当前分支如果放在 git commit 之后才检查，会产生“本地已多一条提交、远端却没同步”的半完成状态；这类问题本质上是 preflight 顺序不对。
- 轻量优化：将 scripts\auto_commit.ps1 的分支检测和 origin 远程预检前移到 commit 之前，缺少关键前提时直接中止，避免制造需要人工回滚或二次补救的本地提交。
- 新方向探索：后续可以把这类“关键前提先检、通过后再执行写操作”的 preflight 模板推广到发布、批处理、浏览器自动化提交等所有有外部副作用的流程。
- 下一步：看 weekly_summary.ps1 和其他定时脚本是否也适合统一成“preflight -> execute -> verify -> explain”的最小稳态模板。

## Self-Evolution Update 16:11

- 学到的点：这条链路里最值得固化的不是某一个单点修补，而是“无人值守脚本必须先做 preflight，再执行写操作，再做 verify，最后给出可解释结果”；否则很容易反复出现不同形态的半完成状态。
- 轻量优化：把 `preflight -> execute -> verify -> explain` 明确写进 `roadmap.md`，作为日报、周报、看板、自动提交等日常链路的统一稳态模板。
- 新方向探索：后续可以补一个通用脚本骨架或检查清单，让新脚本默认带前置检查、结果校验和降级说明，而不是靠每次出问题后再补。
- 下一步：优先看 `weekly_summary.ps1` 是否适合先按这个模板补一层 verify，使周报产出不仅能生成，还能确认统计结果是否完整。

## Self-Evolution Update 16:21

- 学到的点：无人值守汇总脚本如果只负责“写文件”而不做结果校验，就会留下“脚本跑完了，但输出结构不完整”的静默失败窗口；真正稳态需要最小 verify 和可解释结果。
- 轻量优化：给 `scripts\weekly_summary.ps1` 补了最小 preflight / verify / explain：日报目录缺失时直接报清晰错误、周报目录不存在时自动创建、写出后校验关键章节是否存在，并输出 `OK: ... (reports=...; categories=...)` 的可解释结果。
- 新方向探索：后续可以抽一个通用 `task_guard.ps1` 或脚本骨架，把 preflight、verify、状态摘要做成复用模板，减少每条定时链路重复补丁。
- 规则/目标微调：把“能跑”进一步收紧成“能跑且结果可校验”，优先为所有无人值守脚本补最小结果验证而不是继续只修单点异常。
- 下一步：优先看 `build_dashboard.ps1` 和 `auto_commit.ps1` 是否也要统一输出结构化状态摘要，方便后续周报自动汇总成功/回退/失败状态。

## Self-Evolution Update 16:31

- 学到的点：无人值守链路里，`build_dashboard.ps1` 如果只负责写文件、不做最小 preflight 和 verify，就仍会留下“首页已刷新但结构不完整”的静默失败窗口；看板脚本也应该纳入统一稳态模板，而不是只修周报和提交链路。
- 轻量优化：给 `scripts\build_dashboard.ps1` 补了最小 `preflight -> execute -> verify -> explain`：仓库路径缺失时直接报错、周报目录不存在时自动走回退分支、写出后校验 `# Evolution Dashboard` / `## Weekly Overview` / `## Repo` 三个关键章节，并输出结构化成功摘要。
- 新方向探索：下一步可以把这类结构化摘要统一成 `status=ok|fallback|error` 的轻量状态格式，后续周报脚本就能自动汇总各定时任务的真实健康度，而不只是记录“执行过”。
- 规则/目标微调：把“无人值守脚本统一采用稳态模板”进一步细化为“每个脚本默认都要输出机器可读的结果摘要”，这样后续才能做自动健康看板和失败聚合。
- 下一步：优先看 `auto_commit.ps1` 和 `weekly_summary.ps1` 是否也适合统一成同一种结构化状态输出格式。

## Self-Evolution Update 16:41

- 学到的点：自动提交脚本如果只看到 `git push` 返回成功，仍可能留下“本地 HEAD 和远端引用不一致”的静默风险；真正稳态要验证写操作结果，而不是只信执行返回码。
- 轻量优化：给 `scripts\auto_commit.ps1` 补了统一结构化状态输出，并在 push 后追加本地 `HEAD` 与 `origin/当前分支` 的 SHA 对齐校验；无变更时也统一输出 `status=noop`，便于后续周报/看板做机器汇总。
- 新方向探索：后续可以把所有定时脚本的结果统一收敛到 `status=ok|noop|fallback|error`，再自动生成任务健康面板，而不只是记录文字总结。
- 规则/目标微调：把“verify before claiming”进一步具体化为“所有带写操作或外部副作用的脚本，默认在成功后补一层结果对齐校验”。
- 下一步：优先看 `weekly_summary.ps1` 是否也补成同一套结构化状态字段，形成真正一致的无人值守脚本协议。

## Self-Evolution Update 16:51

- 学到的点：当日报、看板、自动提交已经在输出统一的结构化状态时，周报脚本如果仍沿用旧式自由文本成功提示，就会成为链路里唯一不一致的一环，后续难以做自动健康聚合。
- 轻量优化：将 `scripts\weekly_summary.ps1` 的成功输出统一为 `OK: WEEKLY_SUMMARY status=ok; path=...; reports=...; categories=...`，补齐整条无人值守链路的最小机器可读协议。
- 新方向探索：后续可以新增一个轻量 `task_health` 汇总脚本，直接读取 `status=ok|noop|fallback|error` 结果，自动生成任务健康面板，而不是只靠文字日报回顾。
- 规则/目标微调：把“无人值守脚本要可解释”进一步具体化为“优先遵循同一种状态输出协议”，避免每个脚本各说各话。
- 下一步：优先看能否补一个统一的状态字段约定文档或任务健康汇总脚本原型。

## Self-Evolution Update 17:01

- 学到的点：当多个无人值守脚本都开始输出结构化结果时，如果缺少一份明确的“统一状态协议”，后续做健康看板、失败聚合、自动告警时仍会因为字段含义不一致而反复返工。
- 轻量优化：把 `task`、`status=ok|noop|fallback|error`、`path`、`summary` 明确补进 `roadmap.md`，作为后续所有定时脚本共享的最小状态输出模板。
- 新方向探索：下一步可以尝试做一个轻量 `task_health` 汇总脚本，直接读取这些统一状态字段生成任务健康面板，而不是只靠文字周报回顾。
- 规则/目标微调：把“脚本可解释”进一步具体化为“优先遵循统一状态协议”，这样后面新增任务时默认就能接入健康统计。
- 下一步：优先看是否要先做状态协议说明文档，还是直接起一个 `task_health` 原型脚本。

## Self-Evolution Update 17:11

- 学到的点：前面已经把 `task / status / path / summary` 这组最小状态字段写进 roadmap，但如果没有一份可直接复用的输出样例，新脚本接入时仍容易出现字段名不统一、顺序不一致、摘要风格各说各话的问题。
- 轻量优化：新增 `ideas\task-health-summary-template.md`，给无人值守脚本补了一份最小任务健康摘要模板，先统一 4 个核心字段和一行输出示例，降低后续接入成本。
- 新方向探索：后续可以基于这份模板做一个轻量 `task_health` 汇总脚本，自动扫描最近任务输出并生成健康面板，而不只是靠日报人工回看。
- 规则/目标微调：把“优先遵循统一状态协议”再具体一步，收紧为“新增无人值守脚本时，默认先接入最小任务健康摘要模板”。
- 下一步：优先看要不要先做 `task_health` 原型，还是先把现有脚本的输出都对齐到这份模板。

## Self-Evolution Update 17:21

- 学到的点：只有 stdout 状态行还不够稳；后续看板、告警、健康汇总如果都靠解析自由文本，会再次形成隐性耦合。更稳的做法是保留人类可读输出，同时补一个机器可读的最近运行状态文件模板。
- 轻量优化：新增 `ideas\last-run-status-template.json`，定义 `task`、`status`、`path`、`summary`、`timestamp`、`details` 这组最小字段，并把“stdout 状态行 + 可选 JSON 落盘”补进 `roadmap.md`。
- 新方向探索：后续可以让日报、周报、看板、自动提交脚本各自写出 `last-run.status.json`，再由轻量 `task_health` 汇总脚本直接消费这些 JSON，而不是继续解析文本。
- 规则/目标微调：把“统一状态输出协议”进一步细化为“双通道输出”——对人保留简洁状态行，对机器优先提供稳定 JSON。
- 下一步：优先看 `build_dashboard.ps1` 是否适合先试点输出一份 `last-run.status.json`，验证这条链路的接入成本。

## Self-Evolution Update 17:32

- 学到的点：仅靠 stdout 状态行仍然脆弱；后续任务健康看板、失败聚合、自动告警如果继续解析文本，会再次形成格式耦合。先选一条稳定脚本做 JSON 状态落盘试点，比继续空谈协议更有效。
- 轻量优化：给 `scripts\build_dashboard.ps1` 补了 `status\build_dashboard.last-run.json` 输出，沿用 `task / status / path / summary / timestamp / details` 最小结构；脚本执行后已验证 JSON 成功写出，当前状态为 `status=ok`。
- 新方向探索：后续可以把 `weekly_summary.ps1`、`auto_commit.ps1` 也接入同类 `last-run` 文件，再做一个轻量 `task_health` 汇总脚本，直接读 JSON 生成任务健康面板。
- 规则/目标微调：把“统一状态输出协议”进一步落地为“优先让关键无人值守脚本同时产出一行状态文本 + 一份最近运行状态 JSON”，降低后续解析耦合。
- 下一步：优先看 `weekly_summary.ps1` 是否作为第二个接入点，验证这套双通道输出在汇总链路里的复用成本。

## Self-Evolution Update 17:41

- 学到的点：统一状态协议如果只在单个脚本上试点，价值有限；真正能支撑后续健康汇总的是把第二条核心链路也接入同一结构，验证协议是否能跨脚本复用。
- 轻量优化：给 `scripts\weekly_summary.ps1` 补了 `status\weekly_summary.last-run.json` 输出，并在写出后校验 `task=weekly_summary`、`status=ok`；现在周报脚本也具备“状态行 + JSON”双通道结果。
- 新方向探索：后续可以优先把 `auto_commit.ps1` 接成第三个 JSON 状态源，再做一个轻量 `task_health` 汇总脚本，直接读 `status\*.last-run.json` 生成任务健康面板。
- 规则/目标微调：把“新脚本优先接统一状态协议”进一步收紧为“关键无人值守脚本至少两条先跑通双通道输出，再扩到全链路”，避免协议只停留在文档层。
- 下一步：优先看 `auto_commit.ps1` 的最近运行状态 JSON 是否接入成本最低，并验证提交链路是否也适合统一 `details` 字段。

## Self-Evolution Update 17:52

- 学到的点：如果 `build_dashboard.ps1` 和 `weekly_summary.ps1` 已经有 `last-run.json`，但 `auto_commit.ps1` 仍只有 stdout 状态行，那么整条无人值守链路在“最终是否真正落远端”这一环仍缺统一机器可读状态，后续健康汇总会留下最后一个盲区。
- 轻量优化：给 `scripts\auto_commit.ps1` 补了 `status\auto_commit.last-run.json` 输出，统一写入 `task / status / path / summary / timestamp / details`，并在写出后校验 `task=auto_commit` 与 `status` 一致；这样日报、周报、看板、自动提交三条核心链路现在都具备“状态行 + JSON”双通道结果。
- 新方向探索：后续可以直接读取 `status\*.last-run.json` 生成一个轻量 `task_health` 汇总脚本或首页健康区块，不再依赖解析自由文本日志。
- 规则/目标微调：把“关键无人值守脚本先跑通双通道输出”进一步收口为“凡是链路末端负责真实落盘/落远端的脚本，优先补最近运行状态 JSON”，这样看板才能覆盖真正完成态而不是中间态。
- 下一步：优先看要不要直接做 `task_health` 原型，读取 `status\*.last-run.json` 自动汇总 `ok / noop / fallback / error`。

## Self-Evolution Update 18:01

- 学到的点：既然日报、周报、看板、自动提交已经都有 `last-run.json`，继续靠解析文本日志做任务健康汇总就属于重复造耦合；更稳的下一步应该直接消费结构化状态文件。
- 轻量优化：新增 `ideas\task-health-rollup-template.md`，把后续 `task_health` 原型的最小输入/输出、状态优先级和首版范围先固化下来，降低未来接首页健康区块时的实现分歧。
- 新方向探索：下一步可以做一个只读 `status\*.last-run.json` 的轻量健康汇总脚本，自动标记 `ok / noop / fallback / error / missing / stale`，为首页看板增加真实任务健康视图。
- 规则/目标微调：比起继续补更多自由文本总结，优先把“已有结构化状态如何被消费”定义清楚，这样系统会更完整、更稳定。
- 下一步：优先看是否直接起一个 `task_health` 原型脚本，把健康表渲染到首页看板。

## Self-Evolution Update 18:11

- 学到的点：既然系统已经有 `status\*.last-run.json`，真正影响健康看板稳定性的下一个关键点就不是“有没有状态文件”，而是“不同任务的 stale 该怎么算”；如果对周任务和日任务共用同一个过期阈值，后续会出现误报或漏报。
- 轻量优化：新增 `ideas\task-health-freshness-template.md`，补了一份按任务区分 `freshnessBudget` 的最小模板，先把 `build_dashboard / weekly_summary / auto_commit` 的建议时效阈值和 `missing -> error -> stale -> 其余状态` 判定顺序定下来。
- 新方向探索：后续可以让 `task_health` 原型直接消费这套 freshness 规则，稳定输出 `missing / stale / error / fallback / noop / ok`，把首页健康区块真正做成“有状态语义”的看板，而不是仅显示最近一次成功。
- 规则/目标微调：比起继续新增状态字段，下一阶段更应该优先固化“状态如何解释”，尤其是 `stale` 的任务级阈值，避免后续健康判断重新退回硬编码和经验判断。
- 下一步：优先看是否直接实现一个只读 `status\*.last-run.json` + freshness 规则的 `task_health` 轻量原型。

## Self-Evolution Update 18:21

- 学到的点：即使已经有 `status\*.last-run.json` 和 freshness 规则，后续 `task_health` 如果仍靠“扫描到哪些文件就监控哪些任务”来推断范围，还是会留下隐式耦合；新增任务、临时任务、允许 `noop` 的任务都会让健康语义变得含糊。
- 轻量优化：新增 `ideas\task-health-registry-template.json`，给后续健康汇总补一个最小任务注册表模板，显式声明 `task / statusFile / freshnessHours / required / allowNoop`，避免继续靠文件名约定和脚本名猜测监控对象。
- 新方向探索：后续可以让 `task_health` 先读 registry，再读各自 `last-run.json`，这样未来不仅能汇总本仓库核心链路，也能平滑扩展到可选任务、实验任务或多仓库任务组。
- 规则/目标微调：把“统一状态输出 + freshness 规则”再收紧一步，升级为“健康汇总必须同时显式定义任务边界”，优先减少隐式约定，而不是继续堆更多状态字段。
- 下一步：优先看是否直接实现一个首版 `task_health` 原型，验证 registry + `last-run.json` + freshness 的三层输入是否足够稳定。

## Self-Evolution Update 18:32

- 学到的点：只有模板没有执行器，结构化状态、freshness 规则和 registry 仍然只是“设计存在”；真正让系统更完整稳定的关键，是尽快做出一个只读汇总原型，把这些稳定输入真正串起来。
- 轻量优化：新增正式 `scripts\task_health.registry.json` 和可执行 `scripts\task_health.ps1`，按 `registry + last-run.json + freshness` 生成 `status\task_health.last-run.json` 与 `status\task_health.summary.md`；已验证当前 3 个核心任务都能被统一汇总为 `ok`。
- 新方向探索：后续可以把 `task_health.summary.md` 直接接进首页看板，或者继续扩到可选任务/实验任务，形成真正的任务健康区块，而不是只显示“功能是否存在”。
- 规则/目标微调：把“有统一状态协议”进一步收紧为“关键状态必须至少有一个真实消费者”，避免长期停留在模板层、文档层。
- 下一步：优先看 `build_dashboard.ps1` 是否接入 Task Health 区块，把已有结构化健康状态真正展示到首页。

## Self-Evolution Update 18:42

- 学到的点：只把 `task_health` 做成状态文件和汇总 Markdown 还不够；如果首页看板不消费它，这套健康协议仍停留在“后端已存在、前台不可见”，对日常观察和快速排障帮助有限。
- 轻量优化：已将 `scripts\build_dashboard.ps1` 接入 `status\task_health.summary.md`，首页新增 `## Task Health` 区块，并在 `Current Status` 里显示总体健康状态，让已有结构化状态第一次真正进入看板主视图。
- 新方向探索：后续可以继续把 `missing / stale / error` 做成更醒目的看板提示，或者补一个简洁趋势区块，帮助更快发现无人值守链路退化。
- 规则/目标微调：把“关键状态必须至少有一个真实消费者”再具体化为“新增状态源后，优先尽快接到首页或主流程，而不是只停留在日志/模板层”。
- 下一步：优先看是否要把 `task_health.last-run.json` 的 `overall` 和各任务状态做成更紧凑的首页摘要，减少看板阅读成本。

## Self-Evolution Update 19:00

- 学到的点：复杂文档任务（PDF / Word / 简历 / 表格 / Markdown）最常见的失败，不是单个脚本不够强，而是任务一开始没有正确分流，导致把“提取内容”“修 Word 样式”“准备 ATS 简历”“表格转换”混成一条链路，后面就会反复返工。
- 轻量优化：新增 `ideas\document-task-routing-template.md`，把文档类任务按目标分成提取整理、Word/论文排版、简历/求职材料、表格转换、Markdown 转 Office 五条默认路径，并补上统一 preflight、默认路由规则和验收标准。
- 新方向探索：后续可以把这个分流模板继续落成正式 playbook，或补一个简历交付 / PDF 提取 / Word 渲染检查的快速入口脚本，让文档类复杂任务从“能做”变成“默认更稳地做完”。
- 规则/目标微调：把“文档任务优先用 office-local”进一步收紧为“复杂文档任务优先优化分流正确率和验收完整度，而不是只优化单个转换命令”。
- 下一步：优先看是否把这份模板继续扩成“简历与官网投递材料准备”专用 playbook，直接服务求职类复杂任务。

## Self-Evolution Update 19:15

- 学到的点：复杂文档任务里，最值钱的提升不是继续微调某个转换命令，而是先把任务按“交付目标”分流；只有先区分提取、排版、简历、表格、Markdown 交付，后面的工具链和验收才会稳定。
- 轻量优化：已将 `ideas\document-task-routing-template.md` 正式落成 `C:\Users\19766\.openclaw\workspace\playbooks\document-task-routing.md`，并把它接入 `PLAYBOOKS.md` 与 `TOOLS.md`，形成可直接复用的复杂文档任务分流手册。
- 产出物：新增文档任务路由 playbook，内含 5 条默认路径、统一 preflight、默认路由规则、分类型验收标准与回报模板，可直接服务 PDF / Word / 简历 / 表格 / Markdown 交付任务。
- 新方向探索：这类“先分流再执行再校验”的方法同样适合浏览器资料整合与多媒体工作流，后续可继续扩成“简历与官网投递材料准备”专用 playbook。
- 规则/目标微调：把“复杂文档任务优先优化分流正确率和验收完整度”进一步固化为正式工作规则，并在工具说明里明确：混合文档任务先加载 `playbooks\document-task-routing.md`。
- 下一步：优先探索“简历与官网投递材料准备”的复用流程，把资料整理、简历变体、表单包和浏览器填表前置准备进一步打通。

## Self-Evolution Update 19:24

- 学到的点：浏览器查资料类复杂任务的主要损耗，不在“搜索”本身，而在缺少统一收口结构；如果不把已验证事实、页面要求、综合判断、未决问题分开，后续很难继续自动化执行、材料准备或决策支持。
- 轻量优化：新增 `ideas\browser-research-briefing-template.md`，把官网调研、职位信息整合、产品/服务比较、文档转操作方案统一沉淀为可复用简报模板，明确了 source priority、页面记录 schema、4 类信息桶与交付结构。
- 产出物：浏览器研究简报模板，可直接用于“查官网后输出结构化结论 + 来源 + 下一步动作”的任务。
- 新方向探索：后续可以继续把它扩成“职位官网研究与投递前材料准备”专用 briefing/playbook，直接服务求职类复杂任务。
- 规则/目标微调：把“浏览器查资料”进一步收紧为“默认要产出可执行简报，而不是松散网页笔记”，优先提升资料整合到后续执行之间的衔接能力。
- 下一步：优先探索“职位官网研究 -> 材料缺口清单 -> 表单填写前置包”的一体化流程。

## Self-Evolution Update 19:29

- 学到的点：求职类复杂任务里，真正拖慢完成率的常常不是浏览器填表动作本身，而是缺少“官网研究结果如何转成投递前准备包”的固定收口；如果不先把岗位要求、表单约束、现有材料、材料缺口和人工确认项拆开，后面就会一边填一边补，返工很多。
- 轻量优化：新增 `self-evolution-log\ideas\career-application-prep-brief-template.md`，把“职位官网研究 -> 材料缺口 -> Fill Readiness -> 推荐准备动作 -> 可直接复用产物”沉淀成可复用模板；同时把 `playbooks\careers-site-application.md` 补成：当岗位复杂或材料未明显齐备时，先产出短版 `Career Application Prep Brief` 再进入填表。
- 产出物：一份求职投递前准备简报模板，可直接服务官网岗位研究、简历变体准备、form pack 生成和浏览器填表前校验。
- 新方向探索：这条能力值得继续长期投入，后续可继续把 brief 与 `career-form-pack.json`、`resume-variants`、`answer-bank.json` 自动衔接，形成更稳的求职执行链路。
- 规则/目标微调：对官网求职类复杂任务，默认先做“准备度判断（Fill Readiness）”，再决定是否进入浏览器填表，优先减少中途返工而不是只追求尽快开始填。
- 下一步：优先探索“Prep Brief -> 自动生成材料缺口清单 / 简历变体需求 / form pack 更新建议”的半自动衔接能力。

## Self-Evolution Update 19:44

- 学到的点：复杂真实任务里，浏览器自动化真正容易断开的地方，不是网页按钮本身，而是“网页上传控件 -> Windows 文件选择框 -> 回到页面验收”这一段跨上下文交接；如果没有先锁定文件路径和页面验收信号，就很容易出现“桌面操作成功了，但实际并没上传成功”的假完成。
- 轻量优化：新增 `self-evolution-log\ideas\browser-desktop-upload-handoff-template.md`，把上传动作统一拆成 `preflight -> handoff -> verify`，并明确两条默认路线：`browser-native` 与 `browser -> windows-dialog`。
- 产出物：一份可复用的浏览器/桌面上传交接模板；同时把关键规则接入 `playbooks\desktop-operations.md` 和 `playbooks\careers-site-application.md`，后续做官网投递、表单附件上传时可以直接复用。
- 新方向探索：这个方向值得继续长期投入，下一步可以继续把“上传前锁定唯一目标文件 + 上传后页面侧验收”沉淀成更完整的标准流程，服务更多官网站点与表单任务。
- 规则/目标微调：对所有浏览器流程里的附件上传，默认把上传视为独立子任务，不再把桌面对话框操作成功等同于上传成功；必须回到页面侧核验文件名或状态变化。
- 下一步：优先把这套上传交接规则正式化成独立 playbook，方便在求职投递、表单上传、资料提交流程里稳定复用。

## Self-Evolution Update 19:49

- 学到的点：上传类复杂任务最需要的不是再多一条脚本，而是让“浏览器里点上传”和“Windows 里选文件”之间有一个稳定、可复用、可验收的交接面；否则同类任务会在不同网站上重复摔在同一个坑里。
- 轻量优化：已将上传交接模板正式落成 `playbooks\browser-upload-handoff.md`，并接入 `PLAYBOOKS.md`，作为浏览器上传本地文件时的专用流程手册。
- 产出物：新增正式 playbook，内含触发条件、两条上传路线、页面侧验收清单、常见失败模式和可复用回报模板，可直接服务官网投递、表单附件上传、浏览器中的本地文件提交流程。
- 新方向探索：这条能力值得继续长期投入；下一步可继续探索“上传目标文件预检 + 页面上传成功信号识别”的半自动标准化，进一步提升浏览器复杂任务完成率。
- 规则/目标微调：把“上传是独立子任务”进一步固化为正式规则——任何浏览器流程里涉及附件上传时，默认先锁定唯一文件，再选择 browser-native 或 browser->windows-dialog 路线，最后做页面侧核验。
- 下一步：优先探索如何把这套上传交接 playbook 继续接到实际求职投递与表单上传流程里，减少跨上下文上传失败和误判完成。

## Self-Evolution Update 20:00

- 学到的点：生图 / 视频生成类复杂任务里，真正拖慢交付的往往不是模型本身，而是前置需求没有被收口成统一的任务简报；如果一开始不锁定平台、比例、时长、素材来源、风格边界和成功标准，后面就会不断重写 prompt、返工素材和镜头。
- 轻量优化：新增 `self-evolution-log\ideas\multimedia-generation-brief-template.md`，把这类任务统一沉淀为 `Generation Brief -> prompt pack -> acceptance` 三段式流程，并补上静态图、视频、图生视频、混合内容包 4 条默认路线。
- 产出物：一份可复用的多媒体生成任务模板，内含 preflight、Brief schema、提示词脚手架、负向约束模板和验收清单，可直接服务小红书配图、封面图、短视频镜头 prompt、图生视频等任务。
- 新方向探索：这个方向值得继续长期投入；后续可继续把 brief 与“本地素材盘点 / 参考图收集 / 平台适配规格 / 多轮修改记录”打通，形成更稳的多媒体生产工作流。
- 规则/目标微调：对生图 / 视频生成类复杂任务，默认先产出 `Generation Brief` 再写 prompt；优先优化需求收口与验收标准，而不是直接堆更长提示词。
- 下一步：优先探索“素材盘点 -> Generation Brief -> prompt pack -> 版本对比记录”的完整多媒体工作流。

## Self-Evolution Update 20:09

- 学到的点：多媒体复杂任务里，真正可复用的能力不该停在“有一份 brief 模板”，而要进一步落成正式 playbook；只有把素材盘点、约束锁定、prompt 首轮产出和验收标准串成固定流程，后续生图、图生视频、封面图和短视频镜头规划这类真实任务才会稳定完成，而不是每次从零开始重写 prompt。
- 轻量优化：已新增正式 `playbooks\multimedia-generation-workflow.md`，把这类任务统一为 `素材盘点 -> Generation Brief -> prompt pack -> acceptance`，并接入 `PLAYBOOKS.md`，后续命中此类任务时可直接按 playbook 执行。
- 产出物：一份可复用的多媒体生成工作流 playbook，内含 4 条默认路线（静态图 / 视频 prompt pack / 图生视频 / mixed content pack）、Generation Brief 模板、首轮 prompt pack 规则、常见失败模式与验收清单。
- 新方向探索：这个方向值得继续长期投入；下一步优先补“版本对比记录 + 可复用素材清单”，让多轮生成任务不再散落在聊天上下文里。
- 规则/目标微调：对复杂生图 / 视频生成任务，默认先走 playbook，再出 prompt；优先提升交付稳定性、素材复用和验收清晰度，而不是只把提示词写得更长。
- 下一步：优先探索“版本对比记录 + 素材清单”的多媒体资产管理补丁，补齐从首轮生成到多轮迭代的最后一段断层。

## Self-Evolution Update 20:20

- 学到的点：多媒体任务做到第二轮以后，真正容易丢失的不是 prompt 本身，而是“哪个版本为什么更好、哪些素材值得保留、下一轮只该改哪一轴”；如果这层比较不被记录，复杂任务会不断回到凭感觉重写 prompt 的低效状态。
- 轻量优化：新增 `self-evolution-log\ideas\multimedia-iteration-log-template.md`，并把 `playbooks\multimedia-generation-workflow.md` 升级为 `素材盘点 -> Generation Brief -> prompt pack -> iteration log -> acceptance`；同时补上 iteration log 触发条件与模板入口。
- 产出物：一份可复用的多媒体版本对比记录模板，内含版本表、单版本备注、对比结论、下一轮单一改动轴、Reusable Assets 清单，可直接服务生图、视频 prompt、图生视频等多轮生成任务。
- 新方向探索：这个方向值得继续长期投入；下一步可继续探索“版本记录 -> 最终交付包 / 素材库 / 可复用 prompt 片段”的更稳定落盘方式。
- 规则/目标微调：对复杂多媒体任务，不再只保留最后一个 prompt；默认保留 `brief + 版本对比结论 + 可复用素材清单`，优先提升多轮迭代的连续性和复用率。
- 下一步：优先探索多媒体任务的“最终交付包 + 资产归档”轻量规范，让生成结果更容易被后续任务继续复用。
## Self-Evolution Update 20:29

- ѧ���ĵ㣺������ิ����������ȱ���������ǡ�����һ��ҳ�桱������ȱ�ٰѶ�ҳ֤���ȶ��տڳɿɼ���ִ�еļ����̣������ʵ��ҳ��Ҫ���ۺ��жϡ�δ���������һ�𣬺����ͺ����޷�ӵ���ְ׼����������������Ʒ�ԱȻ��ĵ�ת����������ʵ����
- �����Ż����ѽ� `self-evolution-log\ideas\browser-research-briefing-template.md` ��ʽ��� `C:\Users\19766\.openclaw\workspace\playbooks\browser-research-briefing.md`�������� `PLAYBOOKS.md` �� `TOOLS.md`���á���ҳ������о� -> �ṹ���� -> ��һ��ִ�С���ΪĬ�Ͽɸ������̡�
- �����һ����ʽ��������о� briefing playbook���ں�����ģʽ��quick-answer / briefing / comparison / action-pack������Դ���ȼ���֤�ݲɼ� schema��4 ����ϢͰ��4 ���߼�ֵ·�ߡ���֤�嵥�����ģ�壬��ֱ�ӷ���������С�ְλ�о�����Ʒ�Ƚϡ��ĵ�ת���������ȸ�������
- �·���̽������������ֵ�ü�������Ͷ�룻��һ�����Ȱ� `browser-research-briefing` �� `careers-site-application` ��׼����·������ͨ��̽����Research Brief -> Career Application Prep Brief / form pack / fill-data���İ��Զ��νӡ�
- ����/Ŀ��΢�����Զ�ҳ������о����������С�����/FAQ/�ĵ�����������Ĭ���Ȳ�������Դ��Լ�����ۺ��жϺ���һ�������� Research Brief��������ͣ������ɢ��ҳ�ʼǡ�
- ��һ��������̽����������о������һ���ṹ������ְ׼���͸��ӹ��������ǰ�����ϰ�����١��������ϻ�Ҫ��������һ�顱��һ���ظ��Ͷ���

## Self-Evolution Update 20:39

- 学到的点：官网求职类复杂任务里，真正影响完成率的常常不是“浏览器怎么点”，而是“职位页研究结果如何稳定交接成简历变体、form pack、fill-data 和答案素材”；如果没有这层 handoff，后面很容易一边填表一边补材料，返工很多。
- 轻量优化：新增正式 playbooks\career-prep-handoff.md，把官方岗位研究后的下一步固定为 职位页 -> Prep Brief -> 材料缺口 -> 派生产物 -> Fill Readiness，并接入 PLAYBOOKS.md、TOOLS.md 与 playbooks\careers-site-application.md。
- 产出物：一份可复用的求职准备交接 playbook，内含触发条件、5 段式标准循环、
eady | needs-materials | needs-confirmation | not-fit 判定、
esume variant / career-form-pack.json / fill-data.json / answer-bank.json 的派生规则，以及可直接复用的输出模板。
- 新方向探索：这条能力值得继续长期投入；下一步优先探索“Prep Brief -> 自动更新 career-form-pack / fill-data / answer themes”的半自动衔接，进一步减少官网投递前的人肉整理成本。
- 规则/目标微调：对官方招聘官网任务，如果从职位页阅读开始且材料并不明显齐备，默认先做 career-prep-handoff，再进入实际浏览器填表，而不是直接开填。
- 下一步：优先验证这套 handoff 能否在一条真实官网岗位链路里稳定复用，并继续探索从岗位要求自动生成材料缺口清单与答案主题提示。

## Self-Evolution Update 20:49

- 学到的点：官网投递里，“有了职位页研究”到“真正能改出可投递简历”之间还缺一层稳定 handoff；如果不先把 JD 要求映射到已有真实经历，再决定强调点和弱项处理，简历定制很容易退化成泛泛改写，既影响 ATS 对齐，也会让后续问答和填表继续返工。
- 轻量优化：新增正式 `playbooks\resume-tailoring-from-jd.md`，把目标岗位简历定制沉淀为 `JD -> evidence map -> emphasis plan -> resume variant brief -> answer themes` 流程，并接入 `PLAYBOOKS.md` 的官网投递推荐链路。
- 产出物：一份可复用的 JD 定向简历定制 playbook，内含输入检查、证据强度分级（strong / partial / missing）、强调策略、简历变体 brief 模板、答案主题提取与验收标准，可直接服务官网投递前的简历变体准备。
- 新方向探索：这条能力值得继续投入；下一步优先探索如何把 `resume-tailoring-from-jd` 与 `jobs\resume-variants\`、`answer-bank.json`、`career-form-pack.json` 做更顺滑的半自动衔接。
- 规则/目标微调：对有明确目标岗位的求职任务，默认先做 evidence map，再改简历；优先做“真实证据驱动的强调”，而不是泛化重写或暗示不存在的经历。
- 下一步：优先验证这套 playbook 在一条真实 JD -> 简历变体 -> 官网填表链路里的复用效果。

## Self-Evolution Update 21:00

- 学到的点：求职类复杂任务里，简历“改完”并不等于“可投递”；真正高频出错的是最后一段交付收口——多份简历并存、上传文件选错、导出文件未核实、浏览器上传前没有唯一目标文件。这一段如果不固定，前面的 JD 研究、简历定制、表单准备都会被最后一步反复返工。
- 轻量优化：新增正式 `playbooks\resume-delivery-pack.md`，把岗位定向简历的末端流程固定为 `简历变体 -> 交付文件 -> 输出校验 -> 上传交接`，并接入 `PLAYBOOKS.md`、`TOOLS.md` 与官方求职推荐链路。
- 产出物：一份可复用的简历交付包 playbook，内含 source-of-truth 选择、命名规则、最小交付文件集、输出校验清单、upload-ready handoff 模板，可直接服务官网投递附件准备、招聘邮件发送前文件校验和浏览器上传前收口。
- 新方向探索：这个方向值得继续长期投入；下一步优先验证“resume-delivery-pack -> browser-upload-handoff”在真实官网投递中的复用效果，再决定是否补半自动导出/命名脚本。
- 规则/目标微调：对岗位定向简历任务，不再以“文本已改好”作为完成标准；默认要明确一个可编辑源文件、一个首选上传文件，并在进入任何真实投递流程前先核对本地输出路径。
- 下一步：优先在一条真实岗位链路里验证这套交付收口是否能稳定减少错文件上传和末端返工。

## Self-Evolution Update 21:10

- 学到的点：浏览器类复杂任务里，“点了下载”不等于“拿到了可用文件”；真正常见的断点在于 PDF/JD/导出表单这类文件经常停留在页面预览、旧文件误判、保存对话框关闭但未真正落盘，导致后续文档处理、官网投递、资料整合都建立在假完成上。
- 轻量优化：新增正式 `playbooks\browser-download-handoff.md`，把这类任务统一为 `下载触发 -> 本地落盘 -> 文件校验 -> 下一流程交接`；同时接入 `PLAYBOOKS.md` 与 `TOOLS.md`，让浏览器下载文件时默认先做本地验证，不再把页面提示当成完成。
- 产出物：一份可复用的浏览器下载交接 playbook，内含两条默认路线（browser-native / browser -> Windows Save 对话框）、本地校验清单、命名规则、常见失败模式、Download Receipt / Download Handoff 模板，可直接服务 PDF 下载、JD 附件抓取、导出表单、模板文件获取等复杂任务。
- 新方向探索：这个方向值得继续投入；下一步优先探索“下载后的自动分流与轻量内容校验”，例如 PDF / DOCX / XLSX 下载后自动进入对应文档处理路径，进一步减少人工判断和错链路。
- 规则/目标微调：对所有浏览器产出本地文件的任务，默认把下载视为独立子任务；完成标准从“点击成功”收紧为“最终本地文件存在、类型正确、时间新鲜、可进入下一流程”。
- 下一步：优先探索“browser-download-handoff -> document-task-routing / career-prep-handoff / multimedia-generation-workflow”的标准衔接规则。

## Self-Evolution Update 21:19

- 学到的点：浏览器复杂任务里，“下载成功”之后经常还会卡在下一步——同样是本地文件，PDF 可能是岗位 JD、政策说明、论文材料，也可能只是要提取内容；如果不按执行目标分流，而只按扩展名拍脑袋选工具，后续就容易停在本地文件、走错链路，或者把求职 / 文档 / 多媒体任务混在一起返工。
- 轻量优化：新增正式 `playbooks\downloaded-file-routing.md`，把“已验证下载文件 -> 按执行目标分流 -> 显式交接下一工作流”沉淀成标准手册，并接入 `PLAYBOOKS.md`、`TOOLS.md` 与 `roadmap.md`。
- 产出物：一份可复用的下载后分流 playbook，内含 `document-task-routing / career-prep-handoff / resume-delivery-pack / multimedia-generation-workflow / archive unpack` 路由矩阵、快速判定规则、就绪校验与 handoff 模板，可直接服务 PDF/JD/模板/导出表单/素材包等真实任务。
- 新方向探索：这个方向值得继续投入；下一步优先探索“已下载文件的轻量内容识别 + 自动建议下一流程”，让浏览器下载后的 handoff 更接近半自动。
- 规则/目标微调：对浏览器下载类任务，完成标准从“本地文件已存在”进一步收紧为“本地文件已验证且已显式路由到下一工作流”；默认不让任务停在下载这一步。
- 下一步：优先探索把 `browser-download-handoff -> downloaded-file-routing -> document/career/multimedia` 串成更顺滑的默认链路。

## Self-Evolution Update 21:29

- 学到的点：很多复杂真实任务的高频断点，不在浏览器按钮或 Office 命令本身，而在跨上下文的 Windows 文件对话框——比如上传时选错文件、导出时另存到了错误目录、对话框关闭了但源应用其实没接收成功。缺少这层交接规则时，浏览器、桌面、文档三类任务都会反复出现“看起来做了，实际上没完成”的假完成。
- 轻量优化：新增正式 `playbooks\windows-file-dialog-handoff.md`，把 Windows `Open / Save / Save As / Select Folder` 对话框统一沉淀为 `preflight -> dialog focus -> least-brittle action -> local verify -> app-side verify` 工作流，并接入 `PLAYBOOKS.md` 与 `TOOLS.md`。
- 产出物：一份可复用的文件对话框交接 playbook，内含三条默认路线（选已有文件 / 保存新文件 / 选文件夹）、本地校验清单、源应用侧验收信号、常见失败模式与 handoff 模板，可直接服务浏览器上传下载、Word/Excel 导出、桌面应用导入导出等复杂任务。
- 新方向探索：这个方向值得继续投入；下一步优先探索“文件对话框动作后的轻量自动验收建议”，例如按任务类型自动提醒检查新鲜时间、扩展名、页面附件 chip、导出 toast 等关键成功信号。
- 规则/目标微调：把“对话框操作成功”与“任务成功”明确拆开；今后凡是出现 Windows 文件对话框的流程，默认都要同时验证本地文件/文件夹结果和源应用接受结果，避免把关闭对话框误判成完成。
- 下一步：优先把这套文件对话框交接规则进一步接到浏览器上传、浏览器下载和 Office 导出类真实链路里，验证复用效果。

## Self-Evolution Update 21:39

- 学到的点：通用浏览器表单任务里，真正拖慢完成率的常常不是“会不会点输入框”，而是缺少一条稳定的执行边界：哪些字段可直接填、哪些必须确认、什么时候算 draft-ready、什么时候才允许真正 submit。没有这层边界，官网申请、资料提交、报名、后台录入这类真实任务很容易在“看起来已经填了”与“实际上还不能安全提交”之间反复返工。
- 轻量优化：新增正式 `playbooks\web-form-execution.md`，把这类任务沉淀为 `数据准备 -> 字段映射 -> 页面侧验证 -> 明确提交边界` 工作流，并接入 `PLAYBOOKS.md`，同时与 `browser-upload-handoff.md`、`windows-file-dialog-handoff.md`、`document-task-routing.md` 形成默认衔接。
- 产出物：一份可复用的通用表单执行 playbook，内含 draft-only / submit-now 边界、4 类字段桶（安全事实 / 可审阅派生文本 / 上传 / 敏感确认）、多步骤表单检查点、页面侧验证清单、常见失败模式与 handoff 模板，可直接服务官网申请、资料上传、活动报名、后台录入等复杂真实任务。
- 新方向探索：这个方向值得继续长期投入；下一步优先探索“从结构化资料自动生成 Form Fill Brief / 字段映射草稿”的半自动衔接能力，进一步减少复杂表单前的人肉整理成本。
- 规则/目标微调：对所有带外部副作用的浏览器表单任务，默认把“已填写”与“可提交”明确拆开；完成标准收紧为“已验证 draft-ready”或“已验证 submitted”，且最终提交必须保留明确的人类批准边界。
- 下一步：优先验证这套 `web-form-execution -> browser-upload-handoff -> windows-file-dialog-handoff` 链路，在一条真实官网表单任务里能否稳定减少假完成和误提交。

## Self-Evolution Update 21:49

- 学到的点：复杂真实任务里，很多失败并不是单个工具不够强，而是任务一开始就选错执行路径——该直接做的被过度委派、该走 ACP 的大任务被硬塞进主线程、该走本地浏览器/桌面/文档工具链的操作又被误当成编码问题，最终造成上下文拥挤、验证变弱和返工。
- 轻量优化：新增正式 `playbooks\openclaw-agent-delegation.md`，把复杂任务的起手分流固定为 `main agent direct / ACP session / local task tool chain` 三条默认路线，并补了 inspect -> route -> verify 流程、快速决策矩阵和回报模板；同时接入 `PLAYBOOKS.md` 与 `TOOLS.md`。
- 产出物：一份可复用的 OpenClaw 任务委派与执行路径选择 playbook，可直接服务大仓库改造、Codex/Claude Code/Gemini 代理式编码、以及浏览器/桌面/文档类复杂任务的起手分流。
- 新方向探索：这个方向值得继续投入；下一步优先探索“复杂混合任务的多 playbook 串联顺序”，例如 research -> prep -> delivery -> form execution 这类跨浏览器/文档/上传链路的默认编排。
- 规则/目标微调：对复杂任务，优先优化“执行路径选择正确率”，再优化单个命令或脚本；错误的第一路由，通常比后续任何小修补都更贵。
- 下一步：优先在一条真实复杂任务里验证这套 `openclaw-agent-delegation` 是否能稳定减少选错工具链、主线程过载和验证缺口。

## Self-Evolution Update 22:00

- 学到的点：很多复杂真实任务的最后一段并不是“生成文件”本身，而是“如何把刚生成/刚下载的本地文件稳定交给下一步”——如果没有明确唯一文件、没有本地校验、没有 Explorer/应用侧验证，就很容易出现错文件上传、旧文件复用、文件虽然存在但人没看到/没打开的假完成。
- 轻量优化：新增正式 `playbooks\local-file-delivery-handoff.md`，把这类任务统一沉淀为 `验证本地文件 -> 选择交接模式（reveal-only / open-for-review / delivery-pack / route-next）-> 验证 Explorer/应用/下一流程结果`；并接入 `PLAYBOOKS.md`、`TOOLS.md` 与 `playbooks\desktop-operations.md`。
- 产出物：一份可复用的本地文件交付接力 playbook，可直接服务简历交付、Word/PDF/XLSX/图片导出后复核、浏览器下载后的本地打开确认、以及上传前锁定唯一交付文件等复杂任务。
- 新方向探索：这个方向值得继续投入；下一步优先探索“本地文件产出后按任务目标自动建议下一交接模式”，例如 review / upload / archive / route-next 的轻量默认判断。
- 规则/目标微调：对所有会产出本地文件的复杂任务，不再把“脚本说已写出文件”当成完成；默认要把文件推进到 `handoff-ready` 状态——路径明确、文件已校验、首选交付文件明确、必要时 Explorer/应用侧已验证。
- 下一步：优先在一条真实文档或简历任务里验证这套 `local-file-delivery-handoff` 是否能稳定减少错文件、旧文件和末端假完成。

## Self-Evolution Update 22:12

- 学到的点：复杂真实任务里，光有一堆单独 playbook 还不够；真正会反复返工的地方，是 research、prep、delivery、form、upload 之间缺少默认串联顺序，导致虽然第一跳路由对了，第二跳开始又临时拼流程。
- 轻量优化：把 4 条高价值默认串联链路正式补进 `playbooks\openclaw-agent-delegation.md`：`官网研究 -> 准备 -> 简历定制 -> 交付 -> 投递`、`浏览器研究 -> 下载 -> 分流 -> 下一流程`、`结构化资料 -> draft-ready 表单 -> 上传/对话框交接`、`本地文件产出 -> 人类可见交付`。
- 产出物：一套可复用的复杂混合任务链路规则，能直接服务官网投递、浏览器下载后文档处理、带上传的多步骤表单、以及本地文件生成后的交付收口。
- 新方向探索：这条能力值得继续投入；下一步优先探索“链路中的关键输入是否能半自动生成”，例如从 Research Brief 直接派生 Prep Brief / fill-data / upload-ready 目标文件。
- 规则/目标微调：对跨浏览器 / 文档 / 上传 / 本地文件交付的复杂任务，不再只做“选对第一本 playbook”；默认还要选对后续串联顺序，优先减少中途接力断层。
- 下一步：优先在一条真实官网投递或下载后处理链路里，验证这套默认串联顺序是否能稳定减少返工。

## Self-Evolution Update 22:19

- 学到的点：复杂真实任务里，一个很高频的损耗不是“不会做”，而是过早把动作成功当成任务完成；尤其在浏览器、Windows 文件对话框、本地文件交付、文档导出这些链路里，最常见的问题是点击成功了、脚本返回了、对话框关了，但真正的最终产物或页面/应用侧结果并没有被证明。
- 轻量优化：新增 `self-evolution-log\ideas\completion-proof-template.md`，把复杂任务统一收口为 5 个最小完成证明字段：`final artifact / end state`、`primary proof signal`、`secondary proof signal`、`false-completion trap`、`handoff-ready condition`；同时把这条规则接入 `playbooks\openclaw-agent-delegation.md`，并写入 `roadmap.md` 作为后续长期进化方向。
- 产出物：一份可复用的 Completion Proof 模板，可直接服务浏览器上传、浏览器下载、Windows 文件对话框、本地文档/简历交付等复杂真实任务，帮助任务从“动作已执行”升级为“结果已被证明”。
- 新方向探索：这条能力值得继续长期投入；后续可把 completion proof 进一步接入 `browser-upload-handoff`、`browser-download-handoff`、`windows-file-dialog-handoff`、`local-file-delivery-handoff`，逐步形成跨 playbook 的统一验收语义。
- 规则/目标微调：对复杂任务，默认不以“动作已执行”作为完成标准，而以“completion proof 已收集”作为完成标准，优先继续压低桌面 / 浏览器 / 文档链路中的假完成。
- 下一步：优先验证这套 completion proof 模板在一条真实浏览器上传或本地文件交付任务里是否能稳定减少误判完成和后续返工。

## Self-Evolution Update 22:31

- 学到的点：复杂真实任务里，一个持续高频的失败不是“不会点/不会跑”，而是过早把中间动作当成完成；浏览器上传、下载、Windows 文件对话框、文档导出、本地文件交付都很容易出现“动作成功但终态未证实”的假完成。
- 轻量优化：已将 `self-evolution-log\ideas\completion-proof-template.md` 正式落成 `playbooks\completion-proof.md`，作为浏览器 / 桌面 / 文档 / 本地文件交接类任务的统一验收覆盖层，并接入 `PLAYBOOKS.md`、`TOOLS.md` 与 `roadmap.md`。
- 产出物：一份可复用的 Completion Proof playbook，内含 5 个核心字段（最终产物/终态、主证明信号、次证明信号、假完成陷阱、handoff-ready 条件）、4 类领域默认验收规则、proof gap 升级规则和回报模板，可直接覆盖上传、下载、文件对话框、文档导出、draft-ready 表单等复杂任务。
- 新方向探索：这条能力值得继续长期投入；下一步优先把 completion proof 继续嵌入 `browser-upload-handoff`、`browser-download-handoff`、`windows-file-dialog-handoff`、`local-file-delivery-handoff` 等高频 playbook，形成跨链路统一验收语义。
- 规则/目标微调：对复杂任务，默认完成标准从“动作已执行”收紧为“最终产物已出现 + 至少一个强证明信号成立 + 已达到 handoff-ready”，继续压低浏览器 / 桌面 / 文档链路中的假完成。
- 下一步：优先验证这套 completion proof 覆盖层在一条真实上传或导出交付任务里的复用效果，再决定是否进一步做成更短的任务内嵌检查清单。
