# Fix Category Standard

问题分类统一使用以下标签：

- `model`：模型切换、上游 provider、速率限制、server_error
- `browser`：浏览器快照、元素失效、页面跳转、登录态
- `automation`：脚本、自动点击、选择器、任务执行失败
- `config`：OpenClaw 配置、provider 配置、凭证、gateway 重启
- `workflow`：任务流程设计、记录方式、报告结构、重复劳动
